<!-- A DrawerLayout is intended to be used as the top-level content view using match_parent for both width and height to consume the full space available. -->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:id="@+id/drawer_layout"
    android:layout_width="match_parent" android:layout_height="match_parent"
    tools:context="com.votapp.fede.votapp.views.HomeActivity">

    <!-- As the main content view, the view below consumes the entire
         space available using match_parent in both dimensions. -->
    <FrameLayout android:id="@+id/container" android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                style="@style/layout_home"
                android:orientation="horizontal">
                <ImageView
                    style="@style/image_home"
                    android:id="@+id/home_image_logo"
                    android:contentDescription="@string/image_home"
                    android:src="@drawable/home_font_awesome" />

                <TextView
                    style="@style/home_welcome"
                    android:textAppearance="?android:attr/textAppearanceMedium"
                    android:text="Hola, Admin!"
                    android:id="@+id/usermsg_welcome"/>

                <Button
                    style="@style/buttonEmergencia"
                    android:background="@drawable/button_emergency"
                    android:text="S.O.S"
                    android:id="@+id/button_emergencia"
                    android:longClickable="true" />

            </LinearLayout>

            <LinearLayout
                style="@style/home_content"
                android:orientation="vertical"
                android:id="@+id/home_content">

                <ListView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:id="@+id/encuestasList"
                    android:clickable="true" />
            </LinearLayout>

            </LinearLayout>

    </FrameLayout>

    <!-- android:layout_gravity="start" tells DrawerLayout to treat
         this as a sliding drawer on the left side for left-to-right
         languages and on the right side for right-to-left languages.
         If you're not building against API 17 or higher, use
         android:layout_gravity="left" instead. -->
    <!-- The drawer is given a fixed width in dp and extends the full height of
         the container. -->
    <fragment android:id="@+id/navigation_drawer"
        android:layout_width="@dimen/navigation_drawer_width" android:layout_height="match_parent"
        android:layout_gravity="start"
        android:name="com.votapp.fede.votapp.views.fragments.NavigationDrawerFragment"
        tools:layout="@layout/fragment_navigation_drawer" />

    <!--<fragment
        android:layout_width="match_parent"
        android:layout_height="match_parent" android:id="@+id/ayuda_fragmenthome"
        android:name="com.votapp.fede.votapp.views.fragments.ayuda"
        /> -->

</android.support.v4.widget.DrawerLayout>
